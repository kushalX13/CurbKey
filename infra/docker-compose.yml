services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: curbkey
      POSTGRES_PASSWORD: curbkey
      POSTGRES_DB: curbkey
    ports:
      - "5432:5432"
    volumes:
      - curbkey_pg:/var/lib/postgresql/data

volumes:
  curbkey_pg:
